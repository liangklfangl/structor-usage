// import selectModalreducer from '../selectors';
import expect from "expect";

// const selector = selectModalreducer;

describe("selectModalreducer", () => {
  it("Expect to have unit tests specified", () => {
    expect("Test case").toEqual(false);
  });
});
